import 'package:flutter/material.dart';

class AppColors {
  // Prevent instantiation
  AppColors._();

  // 🌿 Primary Brand Colors
  static const Color primary = Color(0xFFBEE4D0);      // Main Green
  static const Color primaryLight = Color(0xFFE3F3E6); // Light Green
  static const Color primaryDark = Color(0xFF388E3C);  // Dark Green
  static const Color blueDark = Color(0xFF2D7FEA);  // Dark blue
  static const Color warmDark = Color(0xFFF8BB70);  // Dark blue
  static const Color visibleGreen = Color(0xFF8EE4B7);      // Main Green



  // 🌱 Secondary / Accent
  static const Color secondary = Color(0xFFB2DFDB);
  static const Color accent = Color(0xFF66BB6A);

  // 🤍 Backgrounds
  static const Color scaffoldBackground = Color(0xFFF6F9F7);
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color modalBackground = Color(0xFFF1F8F6);

  // 🖤 Text Colors
  static const Color textPrimary = Color(0xFF1B1B1B);
  static const Color textSecondary = Color(0xFF6B6B6B);
  static const Color textHint = Color(0xFF9E9E9E);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // 🧊 Borders & Dividers
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFEAEAEA);

  // 🚦 Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFE53935);
  static const Color info = Color(0xFF2196F3);

  // 🌫 Disabled / Inactive
  static const Color disabled = Color(0xFFBDBDBD);
  static const Color disabledBackground = Color(0xFFF0F0F0);

  // 🌑 Shadows
  static const Color shadow = Color(0x1A000000); // 10% black
}
